---
title: Developing with Cargo
description: Pure Cargo workflow for NativeLink contributors who prefer it.
---

If you'd rather work with Cargo than Bazel, every crate compiles cleanly:

```bash
cargo build --workspace
cargo test --workspace
cargo run -p nativelink -- /path/to/config.json5
```

Content coming.
