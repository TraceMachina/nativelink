---
title: Working on documentation
description: Edit, preview, and ship changes to nativelink.com/docs.
---

The docs you're reading live in `web-next/apps/docs/content/docs/`. Edits are
plain MDX — frontmatter + content. Local preview runs alongside the
marketing site:

```bash
cd web-next
bun dev:docs   # docs only — http://localhost:3001/docs
```

Content coming.
