module(
    name = "turbo-cache",
    version = "0.0.0",
    compatibility_level = 0,
)

bazel_dep(name = "platforms", version = "0.0.6")
bazel_dep(name = "rules_cc", version = "0.0.6")

bazel_dep(name = "rules_ll", version = "upstream")
local_path_override(module_name = "rules_ll", path = "../rules_ll")
