---
linters:
  enable-all: true
  disable:
    # Deprecated.
    - exportloopref

    # Allow all packages for now.
    - depguard

    # TODO(aaronmondal): Fix these at some point.
    - exhaustruct
