---
linters:
  enable-all: true
  disable:
    # Deprecated.
    - nosnakecase
    - interfacer
    - exhaustivestruct
    - ifshort
    - deadcode
    - varcheck
    - golint
    - maligned
    - scopelint
    - structcheck
    - execinquery
    - gomnd

    # Allow all packages for now.
    - depguard

    # TODO(aaronmondal): Fix these at some point.
    - exhaustruct
