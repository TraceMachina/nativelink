[build]
rustflags = ["-C", "target-cpu=native", "-C", "link-arg=-fuse-ld=mold"]

# Override workspace Cargo.toml release profile for faster local builds.
# Full LTO + codegen-units=1 is ~10min; thin LTO + 8 CGUs is ~3-4min.
[profile.release]
lto = "thin"
codegen-units = 8
