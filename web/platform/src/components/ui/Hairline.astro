---
/**
 * Faded 1px divider that fits the design system's section rhythm.
 */
interface Props {
   class?: string;
}
const { class: className }: Props = Astro.props;
---

<div
   role="separator"
   aria-orientation="horizontal"
   class:list={["hairline", className]}
></div>
