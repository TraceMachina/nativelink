.next/
out/
.turbo/
.source/
next-env.d.ts
*.tsbuildinfo

# Generated from the repository-root CHANGELOG.md by scripts/gen-changelog.mjs
# (runs as part of `dev` and `build`).
content/docs/reference/changelog.md

# Generated by scripts/gen-llms-txt.mjs (runs as part of `dev` and `build`).
# The store list is read from the committed lib/store-specs.json snapshot, so
# this needs no cargo; refresh the snapshot with `gen:llms-txt -- --schema`.
public/llms.txt
schema.json
